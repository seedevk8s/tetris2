# 테트리스 — 구현 계획

이 프로젝트는 두 단계로 나눠 진행합니다.

| | 내용 | 상태 |
|---|---|---|
| **Phase 1** | 정적 테트리스 게임 — 게임 로직 · BGM · GitHub Pages 배포 | **완료** (2026-08-30) |
| **Phase 2** | Supabase 백엔드 — GitHub 로그인 · 점수 저장 · 랭킹 | **계획 수립** (미구현) |

**Phase 1 은 백엔드가 없습니다.** 정적 HTML·CSS·JS 뿐이고 저장되는 값도 없습니다.
그래서 Phase 2 는 기존 백엔드를 옮기는 "전환"이 아니라 **없던 것을 붙이는 신규 도입**입니다.
옮겨올 데이터도, 맞춰야 할 기존 API도 없습니다.

Phase 2 를 붙여도 **Phase 1 은 그대로 돌아가야 합니다.** 로그인·랭킹은 부가 기능이고,
Supabase 가 멈춰도 게임은 멀쩡해야 합니다. 이것이 두 단계를 가르는 기준입니다.

- 플레이 — <https://seedevk8s.github.io/tetris2/>
- 진행 과정의 서술은 `WORKFLOW.md`, 사용법은 `README.md` 입니다.

---

# Phase 1 — 정적 테트리스 게임 (완료)

> 아래 계획대로 만들었고, 계획과 달라진 부분은
> [구현하면서 계획에서 바뀐 것](#구현하면서-계획에서-바뀐-것)에 적어 두었습니다.

## Context

`src/exercise/chjin/day02/tetris/`에는 (계획 시점에) `CLAUDE.md` 한 개만 있었습니다.
누구나 브라우저에서 바로 열어 실행할 수 있는 **테트리스 MVP**를 만듭니다.
고급 기능(홀드, SRS 월킥, 고스트, 사운드, 랭킹 서버)은 넣지 않고,
"블록이 내려오고 · 움직이고 · 줄이 지워지고 · 점수가 오르는" 최소 동작을 완성도 있게 만드는 것이 목표입니다.

확정된 범위(사용자 선택):
- **기능** — 이동/회전/자동낙하/줄 삭제/점수 + **다음 조각 미리보기**, **하드드롭**, 일시정지, 게임오버·재시작
- **디자인** — `day01/markdown_editor`와 같은 다크 토큰 체계(`--ink #0A0C10`, 액센트 `#E3A44A`, IBM Plex)

제외: 홀드, 최고점수 저장, 벽 밀어내기(월킥) 고급 규칙, 모바일 터치 조작.

**추가(2026-08-30)** — BGM. 화면만 있고 소리가 없으니 허전하다는 요청으로,
테트리스의 전통 곡인 **코로베이니키(Korobeiniki)**를 WebAudio 로 재생합니다.
설계는 아래 [BGM](#추가-기능--bgm-코로베이니키) 절에 있습니다.

## 제약 (tetris/CLAUDE.md)

- 라이브러리 금지 — Canvas 2D + DOM API만. 빌드 도구·npm 없음
- HTML·CSS·JS 각각 별도 파일, 인라인 `<style>`/`<script>` 금지
- 게임 루프는 `requestAnimationFrame` + 경과시간 누적 (`setInterval` 금지)
- 보드 상태(2차원 배열)와 렌더링 분리
- 충돌 판정은 **함수 하나**를 모든 이동/회전/낙하가 공유
- 조각 정의는 상수 테이블 한 곳
- 게임 키에 `preventDefault()`
- 작업이 끝나면 `tetris/WORKFLOW.md`에 프롬프트 원문 + 결과 요약 기록 (필수)
- 커밋·푸시는 사용자가 따로 요청할 때만

## 만들 파일

```
day02/tetris/
├── PLAN.md        이 계획서 (구현 전 가장 먼저 저장)
├── index.html     화면 골격 (canvas 2개 + 점수 패널 + 오버레이)
├── style.css      day01 토큰 체계 다크 테마
├── script.js      게임 로직 전부
├── README.md      무엇인지 / 어떻게 실행하는지 / 조작법
├── WORKFLOW.md    프롬프트 기록 (CLAUDE.md 필수 규칙)
├── GITHUB_PAGES.md   배포 절차 (Phase 1 막바지에 추가)
├── SUPABASE.md       Supabase 준비 절차 (Phase 2)
├── DB_DESIGN.md      DB 설계 (Phase 2)
└── test/
    └── logic.test.js   로직 테스트 (개발용, 게임 실행에는 불필요)
```

### 0단계 — PLAN.md 먼저 저장
구현에 손대기 전에 **이 계획서를 `day02/tetris/PLAN.md`로 그대로 저장**합니다.
이후 구현 중 설계가 바뀌면 PLAN.md도 함께 갱신해, 계획과 결과물이 어긋나지 않게 둡니다.
(`WORKFLOW.md`가 "어떻게 도달했는지"의 기록이라면, `PLAN.md`는 "무엇을 만들기로 했는지"의 기준선입니다.)

### index.html
- `<canvas id="board" width="300" height="600">` — 10×20, 셀 30px
- `<canvas id="next" width="120" height="120">` — 다음 조각 미리보기
- 사이드 패널: 점수 / 지운 줄 / 레벨, 조작법 안내
- 게임오버·일시정지 오버레이 `<div id="overlay">` + 재시작 버튼
- 폰트는 Google Fonts(IBM Plex) 링크, 실패해도 시스템 서체로 동작

### style.css
`day01/markdown_editor/style.css`의 `:root` 토큰을 그대로 가져와 재사용
(`--ink`, `--surface`, `--line`, `--text`, `--muted`, `--accent`, `--body`, `--mono`).
보드와 패널을 flex로 가운데 정렬, 창이 좁아지면 패널이 아래로 내려가도록.

### script.js — 구성

상수 구역 (조정 지점을 위쪽에 모음)
```js
const COLS = 10, ROWS = 20, CELL = 30, NEXT_CELL = 24;

const DROP_START = 800;      // 레벨1 낙하 간격(ms)
const DROP_STEP  = 70;       // 레벨이 1 오를 때 줄어드는 간격
const DROP_MIN   = 100;      // 낙하 간격 하한
const LINES_PER_LEVEL = 10;

const SCORE = { 1:100, 2:300, 3:500, 4:800 };  // 한 번에 지운 줄 수별 점수
const SOFT_DROP = 1, HARD_DROP = 2;            // 칸당 보너스

const MAX_DELTA = 100;       // 프레임 간격 상한 — 백그라운드 탭 복귀 시 순간이동 방지
const KICKS = [0, -1, 1, -2, 2];               // 회전이 막혔을 때 밀어 볼 거리
const SPAWN_X = 3;           // 모든 조각을 3번 열에 맞춰 내려보냄

// 조각 정의 — 색 + 회전 4벌의 좌표
const SHAPES = { I:{ color:'#5BC8D6', rots:[[[0,1],[1,1],[2,1],[3,1]], ...] }, O, T, S, Z, J, L };
```
- 조각 회전은 **회전 상태 배열을 미리 펼쳐 두는 방식**(각 조각당 좌표 4벌)으로 둡니다.
  런타임 행렬 회전보다 코드가 짧고, I·O의 예외 처리가 필요 없습니다.

상태
```js
let board;        // ROWS×COLS 배열, 빈 칸은 null, 채워지면 색 문자열
let piece;        // {type, rot, x, y}
let nextType;
let bag = [];     // 7-bag — 섞어 둔 조각을 하나씩 꺼내 씀
let score, lines, level, dropInterval;
let dropTimer, lastTime, paused, gameOver;
```

핵심 함수
| 함수 | 역할 |
|---|---|
| `cellsOf(type, rot, x, y)` | 조각의 절대 좌표 4개를 돌려줌 — 판정·렌더가 공유 |
| `isValid(type, rot, x, y)` | **유일한 충돌 판정.** 벽·바닥·기존 블록 검사 |
| `move(dx, dy)` / `rotate()` / `hardDrop()` | 전부 `isValid`를 거쳐서만 상태 변경 |
| `stepDown()` / `softDrop()` | 한 칸 낙하. 못 내려가면 `lock()` |
| `lock()` | 보드에 굳히고 → `clearLines()` → `spawn()` |
| `clearLines()` | 꽉 찬 행 제거, 점수·레벨 갱신 |
| `nextFromBag()` | 7종을 한 벌씩 섞어 순서대로 내보냄(7-bag) |
| `spawn()` | `nextType`을 현재 조각으로, 새 `nextType` 뽑기. 스폰 위치가 `isValid` 실패면 게임오버 |
| `loop(t)` | `requestAnimationFrame`. 경과시간 누적 → `dropInterval` 넘으면 한 칸 낙하 |
| `draw()` / `drawNext()` | 보드 배열만 읽어 렌더 |
| `showOverlay()` / `togglePause()` | 일시정지·게임오버 화면 |
| `start()` | 보드·점수·상태 초기화 (첫 실행과 R 재시작이 같은 경로) |

- 회전 시 벽에 걸리면 좌/우 한 칸씩만 밀어보는 **간단한 킥**을 넣습니다(±1, ±2).
  SRS 전체를 구현하지 않되, 벽 옆에서 회전이 안 되는 답답함은 없앱니다.
- 레벨은 지운 줄 10줄마다 +1, `dropInterval = max(100, DROP_START - (level-1)*70)`.

조작
| 키 | 동작 |
|---|---|
| ← → | 좌우 이동 |
| ↓ | 소프트드롭 (칸당 +1점) |
| ↑ | 회전 |
| Space | 하드드롭 (칸당 +2점, 즉시 고정) |
| P | 일시정지 |
| R | 재시작 |
| M | 음악 켜기/끄기 |

`keydown`에서 위 키에 `preventDefault()` — 방향키/스페이스 스크롤 방지.
`R`·`P`는 일시정지·게임오버 중에도 받고, 나머지 키는 그때 무시합니다.
탭을 벗어나면(`visibilitychange`) 자동으로 일시정지합니다.

## 추가 기능 — BGM (코로베이니키)

### 무엇을 어떻게 소리 내나

**오디오 파일을 쓰지 않습니다.** mp3·ogg 를 넣으면 (1) 저장소에 바이너리가 들어가고,
(2) `file://` 로 열 때 브라우저에 따라 로드가 막히며, (3) "라이브러리·에셋 없이 코드로만"이라는
이 과제의 성격에서 벗어납니다. 대신 **WebAudio 로 음을 직접 합성**합니다.

- 멜로디 — `OscillatorNode` **square** 파형 (칩튠 특유의 소리)
- 베이스 — **triangle** 파형, 마디 첫 음을 2옥타브 내려 깔아 줌
- 음마다 `GainNode` 로 짧은 어택 + 릴리즈를 줘서 딸깍거리는 클릭음을 없앰

곡(코로베이니키)은 19세기 러시아 민요로 **퍼블릭 도메인**입니다.

### 곡 데이터

`[음이름, 박자]` 배열 하나로 둡니다. 4분음표 = 1박, 템포 150 BPM (`0.4초/박`).

```js
const MELODY = [
  ['E5',1],['B4',.5],['C5',.5],['D5',1],['C5',.5],['B4',.5],   // A 파트 1마디
  ...
  ['G#5',4],                                                    // B 파트 마지막
];
```

- **A 파트 8마디 + B 파트 8마디**, 합쳐 64박(약 25.6초)이고 끝나면 처음으로 돌아갑니다.
- 주파수는 음이름을 파싱해 `A4=440` 기준 평균율로 계산합니다(`freq('G#5')`).
  음이름→주파수 표를 손으로 적으면 오타를 잡을 방법이 없습니다.
- **베이스는 별도 데이터를 두지 않습니다.** 각 마디 첫 음을 2옥타브 내려 그 마디 내내 깝니다.
  화음을 따로 적어 넣으면 멜로디와 어긋날 여지가 생기는데, 멜로디에서 파생시키면 항상 어울립니다.

### 언제 울리나 — 브라우저 자동재생 정책

브라우저는 **사용자 조작 없이 소리를 내지 못하게** 막습니다.
페이지를 열자마자 `AudioContext` 를 만들면 `suspended` 상태로 시작해 아무 소리도 안 납니다.

그래서 **첫 키 입력이나 클릭이 있을 때 `AudioContext` 를 만들고 `resume()`** 합니다.
게임은 어차피 키를 눌러야 시작되므로 사용자가 이질감을 느낄 지점이 없습니다.
`AudioContext` 자체가 없는 브라우저면 조용히 넘어가고 게임은 그대로 동작합니다.

### 언제 멈추나

| 상황 | 처리 |
|---|---|
| 일시정지(P) · 탭 이탈 | `ctx.suspend()` — 예약된 음까지 즉시 멈춤 |
| 게임오버 | `ctx.suspend()` |
| 재시작(R) | 곡을 **처음부터** 다시 |
| 음소거(M / 버튼) | 마스터 게인을 0 으로. 곡의 진행 위치는 유지 |

음소거와 일시정지를 다르게 처리하는 이유 — 음소거는 "소리만 끄기"라 곡이 흐르던 자리를 지켜야 하고,
일시정지는 게임이 멈춘 것이므로 곡도 그 자리에 멈춰야 자연스럽습니다.
`suspend()` 중에는 `ctx.currentTime` 도 함께 멈추므로 예약 시각 계산이 어긋나지 않습니다.

### 스케줄링 — 타이머를 새로 만들지 않는다

WebAudio 는 보통 `setInterval` 룩어헤드 스케줄러를 씁니다. 하지만 이 프로젝트에는
**이미 `requestAnimationFrame` 루프가 돌고 있으므로** 거기에 얹습니다.

```js
// loop() 안에서
if (audioCtx && !paused && !gameOver) scheduleMusic();

function scheduleMusic() {
  // 앞으로 LOOKAHEAD(0.2초) 안에 울릴 음을 미리 예약
  while (nextNoteTime < audioCtx.currentTime + LOOKAHEAD) { ... }
}
```

음의 **재생 시각은 오디오 시계(`ctx.currentTime`)로 예약**하므로 프레임이 끊겨도 박자는 밀리지 않습니다.
타이머를 하나 더 두지 않아 정지·재개 처리도 게임 상태 한 곳에서 끝납니다.

### 화면

패널에 음악 토글 버튼(`♪`)을 넣고, `M` 키로도 같은 동작을 합니다.
버튼에는 `aria-pressed` 로 상태를 실어 스크린 리더에서도 켜짐/꺼짐을 알 수 있게 합니다.

### 검증

곡 데이터는 눈으로 보기 어려우므로 `test/logic.test.js` 에 확인 항목을 더합니다.

- 모든 음이름이 파싱되고 주파수가 20~4000Hz 안에 드는가 (오타 잡기)
- **마디 합이 정확히 4박씩 떨어지는가** — 한 음의 박자를 잘못 적으면 이후 마디가 통째로 밀리고,
  베이스가 마디 첫 음을 짚는 방식이라 베이스까지 어긋납니다
- 총 길이가 A 8마디 + B 8마디 = 64박인가
- `freq('A4') === 440`

**결과** — 테스트 90 → **206항목, 전부 통과**. 곡 데이터는 이렇게 검증했지만
음색·음량이 귀에 어떻게 들리는지는 취향의 문제라 실제로 들어보고 판단할 몫입니다
(`MUSIC_VOLUME` 한 줄로 조정됩니다).

---

### README.md
`day01/markdown_editor/README.md` 형식을 따름 — 파일 표, 실행 방법, 조작법, 점수 규칙,
"의도적으로 넣지 않은 것"(홀드·고스트·SRS·사운드) 항목.

## 검증

### 계획 시점의 체크리스트

1. 조각 7종이 모두 나오고, 각 조각이 4가지 회전 상태로 정상 회전
2. 좌우 벽·바닥·쌓인 블록을 통과하지 않음 (벽 옆에서 회전 시도 포함)
3. 한 줄 / 여러 줄 동시 삭제 시 점수가 표 대로 오르고 윗 줄이 정확히 내려옴
4. 하드드롭이 정확히 착지 지점에 고정되고 보너스 점수 반영
5. 방향키·스페이스로 페이지가 스크롤되지 않음
6. 맨 위까지 쌓이면 게임오버 오버레이 → R/버튼으로 초기 상태 복귀
7. 레벨이 오르면 낙하가 실제로 빨라짐
8. 탭을 백그라운드로 보냈다 돌아왔을 때 조각이 순간이동하지 않음 (누적 시간 상한 처리)

### 실제로 한 것

**작업 환경에 브라우저가 없어** 화면을 눈으로 확인할 수 없었습니다.
그래서 눈이 필요 없는 항목(1·2·3·4·6·7)은 **로직만 Node 로 떼어내** 자동 검증했고,
눈이 필요한 항목(5·8과 렌더링·레이아웃)은 수동 확인으로 남겼습니다.

```bash
node test/logic.test.js     # 206 passed, 0 failed  (BGM 추가 전 90항목)
```

| 체크 | 방법 | 결과 |
|---|---|---|
| 1 조각·회전 | 7종 × 4벌이 각각 4칸이고 서로 다른가 (O 는 같아야 함) | 통과 |
| 2 충돌 | 좌·우 벽 밖, 바닥 밖, 쌓인 블록과 겹침 거부 | 통과 |
| 3 줄 삭제 | 1~4줄 점수, 삭제 후 보드 상태, 윗 줄이 한 칸 내려오는지 | 통과 |
| 4 하드드롭 | O 조각이 y=0 에서 18칸 낙하 → 착지 위치와 보너스 36점 | 통과 |
| 6 게임오버 | 스폰 자리를 막고 `spawn()` 호출 | 통과 |
| 7 레벨업 | 10줄에서 레벨 2, 간격 730ms, 하한 100ms 에서 멈춤 | 통과 |
| — 회전 킥 | 왼쪽 벽에 붙은 세로 I 를 회전 | 통과 |
| — 7-bag | 7번 뽑으면 7종이 한 번씩 | 통과 |
| 5 스크롤 | 배포본에서 사용자 확인 | 통과 |
| 8 백그라운드 탭 | 배포본에서 사용자 확인 | 통과 |
| — 렌더링·레이아웃·키 입력 | 배포본에서 사용자 확인 | 통과 |

테스트는 `script.js` 를 **텍스트로 읽어** 최소 DOM 스텁과 함께 실행합니다.
`script.js` 에 `module.exports` 같은 Node 전용 코드를 넣지 않기 위해서입니다
(`day01/markdown_editor/test` 에서 쓴 방식과 같습니다).

### 서버 구동

```bash
python3 -m http.server 8080 --directory src/exercise/chjin/day02/tetris
# http://localhost:8080
```

`index.html` · `style.css` · `script.js` 가 모두 200 으로 응답하는 것을 확인했습니다.
`index.html` 더블클릭(`file://`)으로도 동작합니다 — `fetch` 를 쓰지 않아 CORS 에 걸릴 곳이 없습니다.

### 구현 중 잡은 문제

| 문제 | 원인 | 처리 |
|---|---|---|
| 재시작 버튼을 누른 뒤 Space 를 누르면 다시 재시작됨 | 버튼에 포커스가 남아 Space 가 클릭으로 전달 | 클릭 후 `btnRestart.blur()` |
| 탭에 돌아오면 조각이 여러 칸 순간이동 | 누적된 경과시간이 한꺼번에 반영 | `MAX_DELTA` 100ms 로 프레임 간격을 자르고, 탭 이탈 시 자동 일시정지 |
| 방향키·스페이스로 페이지가 스크롤 | 브라우저 기본 동작 | 게임 키에 한해 `preventDefault()` |
| 줄 삭제 루프가 한 줄씩 건너뜀 | 배열에서 행을 지우면 인덱스가 밀림 | 아래→위로 순회하며 삭제 시 `y++` 로 같은 줄 재검사 |

## 구현하면서 계획에서 바뀐 것

- **조각 뽑기** — 단순 무작위 대신 **7-bag**(7종을 섞어 한 벌씩 소진). 5줄 늘어날 뿐인데
  같은 조각이 몰리거나 I 가 한참 안 나오는 구간이 사라집니다.
- **테스트 추가** — 이 환경에 브라우저가 없어 화면을 눈으로 확인할 수 없으므로,
  로직만 Node 로 떼어내 검증하는 `test/logic.test.js`를 넣었습니다(90항목 → BGM 추가 후 206항목).
  `script.js` 에는 Node 전용 코드를 넣지 않고, 테스트가 `script.js` 를 텍스트로 읽어 실행합니다.

## 마무리

- `WORKFLOW.md` 에 프롬프트 원문과 결과 요약(선택 근거·막힌 지점·검증 방법)을 기록했습니다. **완료**
- `README.md` 에 실행·조작·점수 규칙·의도적으로 뺀 것을 정리했습니다. **완료**
- 커밋·푸시는 하지 않았습니다 — 사용자가 요청할 때만.

## 배포

`https://seedevk8s.github.io/tetris2/` — 별도 저장소 `seedevk8s/tetris2` 로 GitHub Pages 배포.
절차와 재배포 방법은 `GITHUB_PAGES.md` 에 있습니다.

빌드가 없어 파일을 그대로 올리는 것으로 끝났고, 경로를 처음부터 상대로 쓴 덕분에
`/tetris2/` 하위에 놓여도 고칠 코드가 없었습니다.

---

# Phase 2 — Supabase 백엔드 (예정)

**아직 구현하지 않았습니다.** 준비 절차는 [SUPABASE.md](SUPABASE.md),
DB 설계는 [DB_DESIGN.md](DB_DESIGN.md)에 있습니다.

시작 조건 — 사용자가 `SUPABASE.md` 의 1~7단계(가입·프로젝트 생성·OAuth 앱 등록·SQL 실행)를
끝내고 **Project URL 과 anon key** 를 전달하면 착수합니다.

## 범위

Phase 1 에는 백엔드가 없으므로 **신규 도입**입니다(위 머리말 참고).

| 넣는 것 | 안 넣는 것 |
|---|---|
| GitHub 계정 로그인 | 이메일·비밀번호 로그인 |
| 게임 종료 시 점수 저장 | 실시간 대전 · 리플레이 |
| 전체 랭킹 TOP 10 | 서버 측 점수 검증 |
| 내 최고 기록 · 최근 플레이 | 친구·팔로우 |

## 무너뜨리면 안 되는 원칙 셋

1. **Supabase가 죽어도 게임은 돈다.** 로그인·랭킹은 부가 기능입니다.
   무료 플랜은 일주일가량 요청이 없으면 프로젝트가 멈추므로, 이건 가정이 아니라 **실제로 겪을 상황**입니다.
   네트워크 실패는 전부 잡아서 "랭킹을 못 불러왔습니다" 정도로 표시하고 게임은 계속합니다.
2. **라이브러리를 쓰지 않는다.** `@supabase/supabase-js` 대신 `fetch`로 REST를 직접 부릅니다.
   `CLAUDE.md`의 무의존성 원칙이 유지됩니다. 대신 토큰 파싱·갱신·세션 복원을 직접 써야 합니다.
3. **게임 로직과 백엔드를 섞지 않는다.** `script.js`는 게임만 알고,
   점수 전송은 "게임이 끝났다"는 사실 하나만 넘기는 형태로 연결합니다.

## 파일 구성

기존 파일을 헤집지 않고 **더하는 쪽**으로 갑니다.

| 파일 | 역할 |
|---|---|
| `supabase-config.js` | Project URL · anon key **한 곳에만**. 환경이 바뀌면 이 파일만 교체 |
| `auth.js` | 로그인 시작 · 콜백 처리 · 세션 저장/복원 · 로그아웃 |
| `api.js` | 점수 등록 · 랭킹 조회 · 내 기록 조회 |
| `script.js` | (수정) 게임오버 시 `onGameOver(결과)` 호출 |
| `index.html` · `style.css` | (수정) 로그인 버튼 · 랭킹 패널 · 내 기록 |

`memo` 과제에서 API 주소를 `config.js` 한 파일로 뺐던 것과 같은 구조입니다.
주소를 `api.js`에 하드코딩하지 않습니다.

## 인증 — 라이브러리 없이 직접 다뤄야 하는 것

```
"GitHub 으로 로그인" 클릭
   └→ location.href = `${URL}/auth/v1/authorize?provider=github&redirect_to=${현재주소}`
        └→ GitHub 승인 → Supabase 콜백 → 우리 페이지로 복귀
             주소: .../tetris2/#access_token=...&expires_at=...&refresh_token=...

페이지 로드 시
   ├ location.hash 에 토큰이 있으면 → 파싱해서 저장하고 hash 를 지운다
   ├ 없으면 localStorage 에서 복원
   └ 만료가 임박했으면 refresh_token 으로 갱신
```

직접 처리해야 하는 지점들:

| 지점 | 처리 |
|---|---|
| 토큰이 `#` 뒤에 붙어 옴 | `location.hash` 파싱 후 `history.replaceState`로 주소창 정리 |
| 새로고침하면 로그아웃됨 | `localStorage`에 세션 저장, 로드 시 복원 |
| 토큰 만료(기본 1시간) | `expires_at` 확인 후 `/auth/v1/token?grant_type=refresh_token` |
| 사용자 정보(닉네임·아바타) | JWT 페이로드에서 꺼내거나 `/auth/v1/user` 조회 |

**주소창 정리를 빠뜨리면** 토큰이 URL에 남아 사용자가 그 주소를 복사해 공유할 수 있습니다.

## 화면

패널이 이미 꽉 차 있어 랭킹을 그대로 얹으면 게임 화면이 좁아집니다.
**오른쪽 패널 아래에 접이식 랭킹 영역**을 두고, 좁은 화면에서는 보드 아래로 내려가게 합니다.

```
┌────────────────┬──────────────┐
│                │ [GitHub 로그인]│
│                │ 점수  1,200   │
│    게임 보드    │ 지운줄 8│레벨 2│
│                │ 다음  ┌────┐  │
│                │ 조작 ← → ↑ ↓  │
│                ├──────────────┤
│                │ 랭킹          │
│                │ 1 alice 8,400│
│                │ 2 bob   6,200│
│                │ …             │
│                │ 내 최고 3,100 │
└────────────────┴──────────────┘
```

로그인 전에도 **랭킹은 보입니다**(RLS가 공개 조회를 허용). 로그인 버튼만 다르게 표시합니다.

## 게임과의 접점은 한 곳

`script.js`의 게임오버 처리에서 결과를 넘기는 것 하나뿐입니다.

```js
// spawn() 안, 게임오버가 확정된 자리
if (window.TetrisBackend) {
  TetrisBackend.submitScore({ score, lines, level });   // 실패해도 throw 하지 않음
}
```

`window.TetrisBackend`가 없거나 전송이 실패해도 **게임오버 화면은 그대로 뜹니다.**
`script.js`가 `fetch`나 토큰을 직접 다루지 않게 해서, 백엔드를 들어내도 게임이 남게 합니다.

## 검증 계획

브라우저가 없는 환경이라 **나눠서** 확인합니다.

| 대상 | 방법 |
|---|---|
| 랭킹 정렬·동점 처리, 응답 파싱, 만료 판정 | Node 테스트에 추가 (기존 206항목에 이어서) |
| 요청 URL·헤더 조립 | `fetch`를 스텁으로 갈아끼워 무엇을 보내는지 검사 |
| 실제 로그인·저장·랭킹 | **배포본에서 사용자 확인** (GitHub Pages 기준) |
| RLS가 실제로 막는가 | 남의 `user_id`로 등록을 시도해 403이 나는지 확인 |

마지막 항목을 꼭 합니다. **RLS는 "켰다"가 아니라 "막힌다"를 확인해야 켠 것입니다.**

## 순서

```
0. (사용자) SUPABASE.md 1~7단계 완료 → Project URL · anon key 전달
1. supabase-config.js · auth.js — 로그인/로그아웃만 먼저. 화면에 이름이 뜨면 성공
2. api.js — 랭킹 조회. 로그인 없이도 보이는지 확인
3. 점수 등록 연결 — RLS 거부 케이스까지 확인
4. 내 기록 표시
5. 테스트·문서 갱신 → 커밋 → tetris2 재배포
```

**1단계를 끝내고 한 번 배포해 확인**한 뒤 다음으로 갑니다.
인증은 리다이렉트 주소·CORS처럼 로컬과 배포본이 다르게 동작하는 지점이 많아,
한꺼번에 만들면 어디서 틀렸는지 찾기 어렵습니다.

## CLAUDE.md 에 더할 규약 (Phase 2 구현 시)

- anon key는 커밋해도 되지만 **service_role key와 DB 비밀번호는 절대 커밋하지 않는다**
- 백엔드 호출은 `api.js`·`auth.js`에만. `script.js`에 `fetch`를 넣지 않는다
- 모든 백엔드 호출은 실패를 잡는다 — **실패해도 게임은 계속되어야 한다**

---

# 그 다음 — 아직 단계로 잡지 않은 후보

MVP 범위 밖이라 뺐습니다. Phase 3 으로 묶을 만큼 정해진 것은 없고, 넣는다면 순서는 이렇습니다.

1. **고스트(착지 위치 미리보기)** — `hardDrop()` 의 낙하 계산을 그대로 재사용하면 되므로 가장 쌉니다.
   (BGM 은 Phase 1 에서 구현했습니다 — [추가 기능 — BGM](#추가-기능--bgm-코로베이니키) 절)
2. ~~최고 점수 저장~~ — **Phase 2 로 넘어갔습니다.** `localStorage` 대신 Supabase 로 계정에 저장합니다.
   기기를 바꿔도 유지되고 랭킹까지 됩니다.
3. **홀드(조각 보관)** — 상태 하나(`held`)와 "이번 조각에 이미 썼는가" 플래그가 필요합니다.
4. **모바일 터치 조작** — 키 핸들러와 동작을 분리해 두면 버튼·스와이프를 붙이기 쉽습니다.
5. **SRS 월킥 전체** — 지금의 `KICKS` 배열을 회전 방향·조각별 오프셋 표로 바꾸는 작업입니다.
