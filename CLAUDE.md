# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

`src/exercise/chjin/day02/tetris/` — 부트캠프 공용 저장소(`weable-kosa/kosa-vibecoding-2026-4th`) 안의
개인 작업 폴더입니다. 이 문서의 규칙은 `day02/tetris` 아래에서만 적용됩니다.
공용 저장소이므로 다른 사람의 `src/exercise/*` 폴더는 건드리지 않습니다.

**현재 프로젝트: tetris — 브라우저에서 도는 테트리스 게임.**

---

## 작업 기록 규칙 (필수)

**이 폴더에서 사용자가 입력하는 모든 프롬프트는 `tetris/WORKFLOW.md`에 누적 기록합니다.**

- **프롬프트는 원본 그대로** 저장합니다. 오타·구어체·띄어쓰기를 고치지 않고, 요약하거나 다듬지 않습니다.
  슬래시 커맨드는 커맨드명과 사용자가 덧붙인 인자를 함께 적습니다.
- **결과는 요약해서** 저장합니다. 무엇을 했는지, 왜 그렇게 정했는지, 무엇으로 확인했는지가 남으면 되고
  코드 전문이나 터미널 출력을 통째로 붙이지 않습니다.
- 프롬프트 하나 = `## N. <프롬프트 원문>` 섹션 하나. 번호는 이어서 증가시키고 기존 섹션은 고치지 않습니다.
- 한 턴의 작업이 끝나면 그 턴에서 바로 기록합니다. 나중에 몰아서 쓰지 않습니다.
- 판단이 갈렸던 지점(선택지 비교, 막힌 곳과 원인, 검증 방법)은 결과 요약에 남깁니다 —
  이 기록의 목적은 결과물 설명이 아니라 **어떻게 거기 도달했는지**입니다.

형식은 `../../day01/WORKFLOW.md`와 `../memo/WORKFLOW.md`를 따릅니다
(머리말에 작업일·결과물·제약 → 프롬프트별 섹션 → 최종 구성 → 실행 방법 → 트러블슈팅 표).

---

## git 정책

이 저장소는 부트캠프 공용 저장소이며, 아래 규칙이 **기본 동작보다 우선**합니다.

### 브랜치 — 만들지 않습니다

**`main`에 직접 커밋하고 직접 푸시합니다.** 기능 브랜치도, PR도 만들지 않습니다.
"기본 브랜치에서는 먼저 브랜치를 생성한다"는 일반 지침은 이 저장소에서 **적용하지 않습니다.**
(사용자가 2026-08-30에 명시적으로 선택한 정책입니다. 저장소 다수 관례이기도 합니다.)

브랜치가 필요하다고 판단되면 임의로 만들지 말고 **먼저 물어봅니다.**

### pull — 항상 merge, rebase 금지

```bash
git pull origin main --no-edit      # merge
```

`--rebase`를 붙이지 않습니다. `~/.gitconfig`에 `pull.rebase = false`가 설정되어 있으므로
옵션 없는 `git pull`도 merge로 동작합니다. 이 설정을 바꾸지 않습니다.
`Merge branch 'main' of ...` 커밋이 쌓이는 것은 **의도된 결과**이며 정리 대상이 아닙니다.

이미 푸시된 커밋에 대해 `rebase`·`commit --amend`·`push --force`를 쓰지 않습니다.

### 푸시 순서

푸시가 거부되지 않으려면 순서를 지킵니다 (day01에서 `behind 15`로 거부된 이력 있음).

```bash
git add <경로>
git commit -m "..."
git pull origin main --no-edit      # 반드시 push 전에
git push origin main
```

### 커밋 범위

- 커밋·푸시는 **사용자가 요청할 때만** 합니다. 작업이 끝났다고 알아서 커밋하지 않습니다.
- `git add -A` / `git add .`를 쓰지 않고 **경로를 지정**해 스테이징합니다.
  공용 저장소라 다른 사람의 변경이 섞여 들어갈 수 있습니다.
- 다른 사람의 `src/exercise/*` 폴더는 스테이징하지 않습니다.
- 커밋 메시지는 한글로 쓰고, 무엇을 왜 바꿨는지가 드러나게 씁니다.

---

## tetris — 브라우저 테트리스

빌드 도구 없는 **정적 HTML·CSS·JS**입니다. `index.html`을 브라우저로 열면 바로 돕니다.
(`day01/markdown_editor`와 같은 구성이며, `memo`처럼 서버가 필요한 구조가 아닙니다.)

```
tetris/
├── index.html        화면 골격 (canvas + 점수/다음 조각 패널)
├── style.css         스타일
├── script.js         게임 로직 (보드·조각·루프·입력) + BGM
├── PLAN.md           설계 기준선 — Phase 1(완료) · Phase 2(Supabase, 미구현)
├── README.md         사용법 — 실행·조작·규칙
├── WORKFLOW.md       작업 기록 — 어떻게 거기 도달했는지
├── GITHUB_PAGES.md   배포 절차 (완료)
├── SUPABASE.md       Supabase 준비 절차 (미구현)
├── DB_DESIGN.md      DB 설계 (미구현)
└── test/
    └── logic.test.js 로직 테스트 (개발용, 게임 실행에는 불필요)
```

**작업을 시작하기 전에 `PLAN.md`를 읽습니다.** 설계 의도가 거기 적혀 있고,
**Phase 1(정적 게임, 완료)과 Phase 2(Supabase 백엔드, 미구현)로 나뉘어** 있습니다.
설계를 바꾸는 작업이면 `PLAN.md`도 함께 갱신해 계획과 결과물이 어긋나지 않게 둡니다.

### 실행

`index.html`을 브라우저에서 직접 엽니다. 로컬 서버·설치·의존성 모두 필요 없습니다.
(외부 파일을 `fetch`하는 코드를 넣으면 `file://`에서 CORS로 막히므로, 그런 코드를 넣지 않습니다.)

### 규약

- **라이브러리를 쓰지 않습니다.** 캔버스 2D 컨텍스트와 DOM API만 사용합니다.
  npm·번들러·프레임워크를 끌어들이지 않습니다.
- HTML·CSS·JS를 **각각 다른 파일**로 유지합니다. 인라인 `<style>`·`<script>`를 넣지 않습니다.
- 게임 루프는 `requestAnimationFrame` + 경과시간 누적으로 돕니다.
  `setInterval`로 낙하를 돌리지 않습니다 — 탭이 백그라운드로 가면 타이밍이 어긋납니다.
- **보드 상태와 렌더링을 분리**합니다. 보드는 2차원 배열, 렌더는 그 배열만 읽어 그립니다.
  캔버스에서 색을 읽어 판정하는 식으로 섞지 않습니다.
- 충돌 판정은 **한 곳의 함수**(예: `isValid(piece, x, y, rot)`)를 거칩니다.
  이동·회전·낙하·하드드롭이 각자 경계 검사를 복사해 갖지 않게 합니다.
- 조각 정의(7종 모양·색·회전 상태)는 **상수 테이블 한 곳**에 둡니다. 로직에 흩뿌리지 않습니다.
- 방향키·스페이스는 화면 스크롤을 유발하므로 게임 키에 대해 `preventDefault()`를 부릅니다.
- 점수·레벨·속도 규칙은 코드 상단 상수로 빼서 조정 지점을 한곳에 모읍니다.
- 사용자 입력 문자열을 화면에 넣을 일이 생기면 **`textContent`만** 사용합니다(`innerHTML` 금지).

### BGM 규약

배경음악(코로베이니키)은 **WebAudio로 음을 직접 합성**합니다.

- **오디오 파일을 넣지 않습니다.** mp3·ogg·wav 모두 마찬가지입니다.
  공용 저장소에 바이너리가 들어가고, `file://`로 열 때 로드가 막힐 수 있으며,
  "에셋 없이 코드로만"이라는 이 과제의 성격에서 벗어납니다.
- 곡은 `MELODY` **배열 하나**(`[음이름, 박자]`)에만 둡니다. 로직에 음을 흩뿌리지 않습니다.
- 주파수는 `freq()`로 **계산**합니다. 음이름→주파수 표를 손으로 적지 않습니다 — 오타를 잡을 수 없습니다.
- 베이스는 별도 화음 데이터를 두지 않고 **마디 첫 음을 2옥타브 내려** 파생시킵니다.
  화음 표를 따로 두면 멜로디를 고칠 때 어긋납니다.
- **음악용 타이머를 새로 만들지 않습니다.** 기존 `requestAnimationFrame` 루프 안에서
  `scheduleMusic()`으로 룩어헤드 예약합니다. `setInterval` 스케줄러를 붙이지 않습니다.
- 재생 시각은 **오디오 시계(`ctx.currentTime`)로 예약**합니다. 프레임 시각으로 예약하면 박자가 밀립니다.
- **음소거와 일시정지를 같게 처리하지 않습니다.**
  음소거는 마스터 게인 0(곡은 흐르던 자리 유지), 일시정지는 `ctx.suspend()`(곡도 그 자리에 멈춤).
  게인만 0으로 두면 이미 예약된 음이 계속 흘러 일시정지 후 박자가 앞서 나갑니다.
- `AudioContext`는 **첫 사용자 조작 때** 만듭니다. 브라우저가 자동재생을 막습니다.
  `AudioContext`가 없는 브라우저에서는 조용히 넘어가고 게임은 그대로 동작해야 합니다.
- 음마다 게인으로 어택·릴리즈를 줍니다. 없으면 딸깍거리는 클릭음이 납니다.

### 배포

이 게임은 별도 저장소 `seedevk8s/tetris2` 로 **GitHub Pages 배포되어 있습니다**
(<https://seedevk8s.github.io/tetris2/>). 절차는 `GITHUB_PAGES.md` 를 따릅니다.

- **경로를 절대 경로로 쓰지 않습니다.** Pages 프로젝트 페이지는 주소가 `/tetris2/` 하위로
  들어가므로 `/style.css` 같은 경로는 404 가 납니다. 항상 `style.css` 처럼 상대 경로로 씁니다.
- **수업 저장소가 원본이고 `tetris2` 는 배포용 사본입니다.** `tetris2` 에서 직접 고치지 않습니다.
- 재배포는 수업 저장소에 push 한 **뒤** `git subtree split` 결과를 `tetris2` 로 보냅니다.
  배포용 사본이라 필요하면 `--force` 로 덮어써도 잃을 것이 없습니다.
- 새 파일을 추가할 때 **`index.html`·`style.css`·`script.js` 는 저장소 루트**에 있어야 합니다.

### 백엔드 (Supabase — Phase 2, 아직 구현 전)

로그인과 점수 저장을 Supabase로 붙일 계획입니다. **아직 코드는 없습니다.**
준비 절차는 `SUPABASE.md`, DB 설계는 `DB_DESIGN.md`, 구현 순서는 `PLAN.md` 를 따릅니다.

구현할 때 지킬 것:

- **게임이 백엔드에 의존하지 않습니다.** 로그인·랭킹은 부가 기능이고,
  Supabase가 멈춰도(무료 플랜은 일주일가량 요청이 없으면 정지) 게임은 그대로 돌아야 합니다.
  모든 백엔드 호출은 실패를 잡고, 실패해도 게임을 막지 않습니다.
- **라이브러리를 쓰지 않습니다.** `@supabase/supabase-js` 대신 `fetch` 로 REST를 직접 부릅니다.
  이 폴더의 무의존성 원칙은 백엔드가 붙어도 유지됩니다.
- **`script.js` 에 `fetch` 를 넣지 않습니다.** 백엔드 호출은 `api.js`·`auth.js` 에만 둡니다.
  게임 로직과 통신이 섞이면 백엔드를 들어냈을 때 게임까지 망가집니다.
- 접속 정보는 **`supabase-config.js` 한 파일**에만 둡니다. 주소를 다른 파일에 하드코딩하지 않습니다.
- **anon key 는 커밋해도 됩니다** (공개 전제 키이고 방어는 RLS가 합니다).
  **`service_role` key 와 DB 비밀번호, GitHub Client Secret 은 절대 커밋하지 않습니다.**
- 스키마를 바꿀 때는 `DB_DESIGN.md` 를 **먼저** 고치고 SQL을 실행합니다.

### 검증

브라우저가 없는 환경에서도 확인할 수 있도록 로직 테스트를 둡니다.

```bash
node test/logic.test.js
```

- **`script.js`에 Node 전용 코드를 넣지 않습니다.** `module.exports`를 붙이지 않습니다.
  테스트가 `script.js`를 **텍스트로 읽어** 최소 DOM 스텁과 함께 실행합니다.
- 곡 데이터를 고치면 **마디 정렬 검사**가 반드시 통과해야 합니다.
  박자를 하나 잘못 적으면 이후 마디가 통째로 밀리고, 마디 첫 음을 짚는 베이스까지 어긋납니다.
- 렌더링·키 입력·실제 소리는 Node로 확인할 수 없으므로 브라우저에서 직접 봐야 합니다.
