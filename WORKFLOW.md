# 작업 기록 (Prompt & Result Workflow)

테트리스를 만들면서 입력한 프롬프트와 각 단계의 결과를 정리한 기록입니다.

- **작업일** — 2026-08-30
- **결과물** — `index.html` · `style.css` · `script.js` (+ `PLAN.md` · `README.md` · `test/`)
- **BGM** — 코로베이니키를 WebAudio 로 합성 (오디오 파일 없음)
- **제약** — HTML, CSS, JavaScript 만 사용 (외부 라이브러리 금지, 빌드 도구 없음)

---

## 1. day02 폴더 밑에 tetris 폴더를 만들고, 이 폴더를 작업경로로 해줘. day02의 CLAUDE.md를 tetris로 복사해와서 수정하자. 앞으로 만들려고 하는 프로젝트는 tetris야.

`day02/tetris/` 를 만들고 `day02/CLAUDE.md` 를 복사해 프로젝트에 맞게 고쳤습니다.

**그대로 둔 것** — 작업 기록 규칙(프롬프트 원문 누적, 결과는 요약)과 git 정책 전체
(브랜치 만들지 않고 `main` 직접 푸시, pull 은 merge 만, 경로 지정 스테이징, 요청할 때만 커밋).
기록 대상만 `memo/WORKFLOW.md` → `tetris/WORKFLOW.md` 로 바꿨습니다.

**바꾼 것** — memo 의 백엔드/프론트엔드 분리 규약(FastAPI · SQLite · CORS · venv)을 통째로 걷어내고,
tetris 규약을 새로 썼습니다. 이 규약이 이후 구현의 기준이 됐습니다.

| 규약 | 이유 |
|---|---|
| Canvas 2D + DOM API 만 | 과제 조건 (라이브러리 금지) |
| `requestAnimationFrame` + 경과시간 누적 | 탭이 백그라운드로 가면 `setInterval` 은 타이밍이 어긋남 |
| 보드 배열과 렌더링 분리 | 캔버스 픽셀을 읽어 판정하는 구조가 되면 손댈 수 없어짐 |
| 충돌 판정 함수 하나 | 이동·회전·낙하가 각자 경계 검사를 복사하면 어긋남 |
| 조각 정의는 상수 테이블 한 곳 | 색·모양이 로직에 흩어지는 것을 막음 |

**스택 판단** — 서버가 필요한 구성(점수 서버 저장)과 프레임워크를 놓고,
`day01/markdown_editor` 및 저장소 관례를 따라 **빌드 없는 정적 3파일**로 정했습니다.
"누구나 브라우저에서 바로" 라는 다음 요구와도 맞아떨어졌습니다.

---

## 2. 누구나 브라우저에서 바로 실행할 수 있는 간단한 프론트엔드 웹사이트 디자인을 가진 테트리스 어플리케이션을 만들어줘. 고급 기능은 필요없고, 점수 표시, 블록 이동 기능 등 최소한의 기능만 갖춘 MVP 버전이면 충분해.

### 범위를 먼저 물어봤다

"최소한"의 경계가 사람마다 다른 지점이 두 곳 있어 확인하고 시작했습니다.

| 물어본 것 | 선택 |
|---|---|
| 기능 범위 | **핵심 + 다음 조각 미리보기 + 하드드롭** (정말 최소한 / 최고점수 저장 중에서) |
| 디자인 톤 | **day01 과 같은 다크 토큰 체계** (레트로 아케이드 / 밝은 미니멀 중에서) |

다음 조각과 하드드롭은 "고급 기능"이 아니라 **없으면 테트리스가 심심해지는 최소선**이라 판단해
추천안으로 올렸고 그대로 채택됐습니다. 홀드·고스트·SRS 월킥·사운드는 뺐습니다.

### 설계에서 갈렸던 지점

**회전을 어떻게 표현할까** — 두 가지가 있었습니다.

| 방식 | 판단 |
|---|---|
| 런타임 행렬 회전 | 코드는 짧지만 I·O 의 회전 중심이 예외라 결국 특수 처리가 붙음 |
| **회전 4벌을 좌표로 펼쳐 두기** | 채택. 상수 테이블이 길어지는 대신 예외가 0 |

조각 정의를 상수 테이블 한 곳에 모으라는 CLAUDE.md 규약과도 맞습니다.

**조각을 어떻게 뽑을까** — 단순 무작위(`Math.random()`)로 하면 같은 조각이 대여섯 번 연달아 나오거나
I 조각이 한참 안 나오는 구간이 생깁니다. 7종을 한 벌씩 섞어 순서대로 내보내는 **7-bag** 으로 했습니다.
코드가 5줄 더 늘어날 뿐인데 체감 품질 차이가 큽니다. (계획서에는 "무작위로 뽑기"로 적었던 부분을 구현에서 바꿈)

**회전 킥** — SRS 전체를 구현하면 MVP 범위를 넘습니다. 그렇다고 없으면 벽에 붙었을 때
회전이 안 돼 답답합니다. 좌우로 `0, -1, +1, -2, +2` 만 밀어 보는 방식으로 타협했습니다.

### 구조

```
board (ROWS×COLS 배열)  ──→  draw() / drawNext()   배열만 읽어 렌더

move() / rotate() / stepDown() / hardDrop()
        └──→ isValid()   ← 벽·바닥·쌓인 블록 판정이 여기 한 곳뿐

loop(t)  requestAnimationFrame + 경과시간 누적
```

`cellsOf(type, rot, x, y)` 가 조각의 절대 좌표 4개를 돌려주고, **판정과 렌더가 이 함수를 함께** 씁니다.
좌표 계산이 두 곳에 있으면 "보이는 위치와 판정 위치가 다른" 버그가 생깁니다.

화면은 보드(캔버스) + 오른쪽 패널(점수 · 지운 줄 · 레벨 · 다음 조각 · 조작 안내) 배치이고,
일시정지·게임오버는 보드 위에 겹치는 오버레이로 처리했습니다.

```
┌────────────────────┬──────────────┐
│                    │ 점수  1,200   │
│                    │ 지운줄 8│레벨 2│
│      10 × 20       │ 다음  ┌────┐  │
│      게임 보드      │      │ ▣▣ │  │
│                    │      └────┘  │
│                    │ 조작 ← → ↑ ↓  │
└────────────────────┴──────────────┘
```

### 막힌 곳과 처리

| 문제 | 원인 | 처리 |
|---|---|---|
| 다시 시작 버튼을 누른 뒤 Space 를 누르면 게임이 재시작됨 | 버튼에 포커스가 남아 Space 가 클릭으로 전달됨 | 클릭 후 `btnRestart.blur()` |
| 탭에 돌아오면 조각이 여러 칸 순간이동 | 누적된 경과시간이 한꺼번에 반영됨 | 프레임 간격을 `MAX_DELTA` 100ms 로 자르고, 탭을 벗어나면 자동 일시정지 |
| 방향키·스페이스로 페이지가 스크롤됨 | 브라우저 기본 동작 | 게임 키에 대해서만 `preventDefault()` |
| 줄 삭제 루프에서 한 줄씩 건너뜀 | 배열에서 행을 지우면 인덱스가 밀림 | 아래에서 위로 순회하면서 삭제 시 `y++` 로 같은 줄을 다시 검사 |

### 검증 — 브라우저 없이 로직부터

이 환경에는 브라우저가 없어 화면은 눈으로 못 봅니다. 대신 **로직만 Node 로 떼어내 검증**했습니다.
`script.js` 는 브라우저 전용이라 `module.exports` 를 넣지 않았고,
테스트가 `script.js` 를 **텍스트로 읽어** 최소 DOM 스텁과 함께 실행합니다
(`day01/markdown_editor/test` 에서 쓴 방식과 같습니다).

```bash
node test/logic.test.js     # 90 passed, 0 failed
```

확인한 것 — 조각 7종의 회전 4벌이 각각 4칸이고 서로 다른가(O 는 같아야 함),
벽·바닥·쌓인 블록 판정, 1~4줄 삭제 점수와 삭제 후 윗 줄이 정확히 내려오는가,
10줄마다 레벨업하고 낙하 간격이 하한에서 멈추는가, 하드드롭 착지 위치와 보너스,
벽에 붙은 세로 I 가 회전되는가(킥), 스폰이 막히면 게임오버가 되는가, 7-bag 이 7종을 한 번씩 내보내는가.

---

## 3. 계획은 괜찮은데 이 계획을 먼저 같은 폴더에 PLAN.md로 저장하고 진행하자

구현에 손대기 전에 계획서를 `tetris/PLAN.md` 로 저장했습니다.
`WORKFLOW.md` 가 "어떻게 도달했는지"의 기록이라면 `PLAN.md` 는 "무엇을 만들기로 했는지"의 기준선입니다.
구현 중 바뀐 부분(조각 뽑기를 7-bag 으로, 테스트 파일 추가)은 PLAN.md 에도 반영해
계획서와 결과물이 어긋나지 않게 했습니다.

---

## 4. 커밋과 푸시해줘

CLAUDE.md 의 git 정책대로 처리했습니다 — 브랜치·PR 없이 `main` 직접 커밋,
`git add` 에 `src/exercise/chjin/day02/tetris` **경로만 지정**(공용 저장소라 남의 변경이 섞이지 않게),
push 전에 `git pull origin main --no-edit` 로 merge.

커밋 `a250b00` (8 files, +1449) → merge `d18cac4` → push 완료.
pull 로 다른 분들의 day02 작업이 함께 들어왔고, `romiaril` 님도 `day02/tetris` 를 올렸지만
경로가 달라 충돌은 없었습니다.

---

## 5. 혹시 BGM이 없다면 좀 허전하니 테트리스의 전통적인  BGM인 코로베이니키를 WebAudio로 플레이하게 수정해줘. 이 내용도 PLAN.md에 먼저 반영한 후 구현을 진행해줘.

요청대로 `PLAN.md` 에 설계를 먼저 쓰고 구현했습니다.

### 오디오 파일을 쓰지 않기로 한 이유

mp3·ogg 를 넣는 선택지가 있었지만 셋 다 걸렸습니다 —
(1) 공용 저장소에 바이너리가 들어가고, (2) `file://` 로 열 때 브라우저에 따라 로드가 막히며,
(3) "라이브러리·에셋 없이 코드로만"이라는 이 과제의 성격에서 벗어납니다.
**WebAudio 로 음을 직접 합성**하면 셋 다 없습니다. 곡 데이터는 `[음이름, 박자]` 배열 하나뿐입니다.

코로베이니키는 19세기 러시아 민요로 퍼블릭 도메인이라 저작권 문제가 없습니다.

### 설계에서 갈렸던 지점

**주파수를 표로 적을까, 계산할까** — 음이름→주파수 표를 손으로 적으면 오타를 잡을 방법이 없습니다.
`freq('G#5')` 처럼 음이름을 파싱해 `A4=440` 기준 평균율로 계산하도록 했습니다.

**베이스를 어떻게 붙일까** — 화음 진행을 따로 데이터로 두면 멜로디를 고칠 때 어긋납니다.
**각 마디 첫 음을 2옥타브 내려** 그 마디 내내 까는 방식으로 했습니다.
멜로디에서 파생되므로 항상 어울리고, 데이터가 늘지 않습니다.

**스케줄링에 타이머를 새로 둘까** — WebAudio 는 보통 `setInterval` 룩어헤드 스케줄러를 씁니다.
하지만 이 프로젝트엔 이미 `requestAnimationFrame` 루프가 돌고 있어 거기에 얹었습니다.
재생 시각은 오디오 시계(`ctx.currentTime`)로 예약하므로 프레임이 끊겨도 박자는 밀리지 않고,
타이머가 하나뿐이라 정지·재개도 게임 상태 한 곳에서 끝납니다.

**음소거와 일시정지를 같게 볼까** — 다르게 처리했습니다.
음소거는 "소리만 끄기"라 **마스터 게인을 0** 으로 두고 곡이 흐르던 자리를 지키고,
일시정지는 게임이 멈춘 것이므로 **`ctx.suspend()`** 로 곡도 그 자리에 멈춥니다.
`suspend()` 중에는 `ctx.currentTime` 도 함께 멈춰서 예약 시각 계산이 어긋나지 않습니다.
(게인만 0으로 두면 이미 예약된 음이 계속 흘러 일시정지 후에도 박자가 앞서 나갑니다)

### 막힌 곳

| 문제 | 원인 | 처리 |
|---|---|---|
| 페이지를 열어도 소리가 안 남 | 브라우저가 사용자 조작 없는 자동재생을 차단 | 첫 키 입력·클릭 때 `AudioContext` 를 만들고 `resume()` |
| 음 시작·끝에 딸깍거리는 잡음 | 게인이 0에서 갑자기 튀어오름 | 음마다 10ms 어택 + 60ms 릴리즈 |
| 테스트가 `setAttribute is not a function` 으로 실패 | 음악 버튼 상태를 `aria-pressed` 로 쓰는데 DOM 스텁에 없었음 | 스텁에 `setAttribute` 추가 |

### 검증

곡 데이터는 눈으로 훑어서는 틀린 곳을 못 찾습니다. 테스트를 늘렸습니다 (90 → **206항목, 전부 통과**).

- 모든 음이름이 파싱되고 주파수가 가청 범위(20~4000Hz)에 드는가
- `freq('A4')===440`, `freq('A5')===880`, `C5≈523.25`, `G#4≈415.30`
- **마디 경계를 걸치는 음이 없는가** — 박자를 하나 잘못 적으면 이후 마디가 통째로 밀리고,
  마디 첫 음을 짚는 베이스까지 어긋납니다
- 총 64박 = 16마디 = 한 바퀴 25.6초 (150 BPM)
- 마디 첫 음이 모두 실음인가 (쉼표면 베이스가 빠짐)
- `AudioContext` 가 없는 환경에서 `ensureAudio()` 가 조용히 넘어가는가

마디별로 펼쳐 표준 채보와도 대조했습니다.

```
 1마디 | E5  B4·0.5  C5·0.5  D5  C5·0.5  B4·0.5
 2마디 | A4  A4·0.5  C5·0.5  E5  D5·0.5  C5·0.5
 ...
16마디 | G#5·4
```

**실제 소리는 확인하지 못했습니다** — 작업 환경에 브라우저가 없어 재생·음색·음량은 직접 들어봐야 합니다.

---

## 6. 커밋과 푸시해줘

BGM 작업을 커밋 `ac66b92` (7 files, +443 -8) 로 올리고 push 했습니다.
1번 때와 같은 절차 — 경로 지정 스테이징 → 커밋 → `git pull origin main --no-edit` → push.
pull 로 `youn1223` 님의 `day02/tetris` 가 함께 들어왔지만 경로가 달라 충돌은 없었습니다.

---

## 7. 지금까지 작업한 내용들을 필요한 문서들에 모두 반영하고 다시 커밋과 푸시를 해줘

문서 넷을 훑어 **어긋난 곳과 빠진 곳**을 찾아 메웠습니다.

### 빠져 있던 것 — CLAUDE.md 에 BGM 규약이 없었다

가장 큰 구멍이었습니다. `CLAUDE.md` 의 tetris 규약은 캔버스·게임 루프·충돌 판정까지만 다루고 있어서,
이 폴더를 다시 여는 사람(또는 다음 세션의 Claude)이 **오디오를 어떻게 다뤄야 하는지 알 길이 없었습니다.**
BGM 구현에서 판단했던 것들이 코드에만 남아 있으면 다음에 손댈 때 그대로 깨집니다.

`### BGM 규약` 을 새로 넣어 아래를 규칙으로 못 박았습니다.

| 규칙 | 어기면 생기는 일 |
|---|---|
| 오디오 파일을 넣지 않는다 | 공용 저장소에 바이너리, `file://` 로드 차단 |
| 곡은 `MELODY` 배열 한 곳 | 음이 로직에 흩어져 수정이 어려워짐 |
| 주파수는 `freq()` 로 계산 | 표를 손으로 적으면 오타를 잡을 방법이 없음 |
| 베이스는 마디 첫 음에서 파생 | 화음 표를 따로 두면 멜로디 수정 시 어긋남 |
| 음악용 타이머를 새로 만들지 않는다 | 타이머가 둘이 되면 정지·재개 처리가 갈라짐 |
| 오디오 시계로 예약 | 프레임 시각으로 예약하면 박자가 밀림 |
| 음소거(게인 0) ≠ 일시정지(`suspend`) | 게인만 0이면 일시정지 후 박자가 앞서 나감 |
| `AudioContext` 는 첫 조작 때 생성 | 브라우저 자동재생 차단으로 소리가 안 남 |

`### 검증` 절도 함께 넣었습니다 — `script.js` 에 Node 전용 코드를 넣지 않는다는 것,
곡을 고치면 마디 정렬 검사를 반드시 통과해야 한다는 것.

### 어긋나 있던 것 — 테스트 항목 수

BGM 을 넣으며 테스트가 90 → 206 으로 늘었는데 문서 곳곳에 90 이 남아 있었습니다.

| 위치 | 처리 |
|---|---|
| `PLAN.md` 검증 절 | 206 으로 고치고 "(BGM 추가 전 90항목)" 병기 |
| `PLAN.md` 계획에서 바뀐 것 | "90항목 → BGM 추가 후 206항목" 으로 |
| `WORKFLOW.md` 최종 구성 | 206 으로 |
| `WORKFLOW.md` 2번 섹션의 `# 90 passed` | **그대로 둠** — 그 시점의 사실이라 고치면 기록이 아니게 됩니다 |

마지막 항목이 판단이 갈린 지점입니다. `WORKFLOW.md` 는 **시간 순 기록**이라 당시 수치를 보존하고,
`PLAN.md`·`README.md` 는 **현재 상태를 말하는 문서**라 현행화하는 것으로 구분했습니다.

### 그 밖에

`CLAUDE.md` 의 폴더 구성도 3파일에서 실제 구성(`PLAN.md`·`README.md`·`WORKFLOW.md`·`test/`)으로 고치고,
"작업 전에 `PLAN.md` 를 읽고, 설계를 바꾸면 함께 갱신한다"를 명시했습니다.

---

## 8. 이 어플리케이션을 github pages에 배포하는 방법을 정리해서 GITHUB_PAGES.md로 저장해줘. 내 github 계정은 seedevk8s이고 사용할 repo 이름은  tetris2로 할거야. 별도의 repo로 배포하려고 하니 어떤 순서로 해야 하는지 해당 문서에 정리해줘. 네가 할 수 있는 작업과 내가 해야 하는 작업을 분리해서 명기해줘.

`GITHUB_PAGES.md` 를 썼습니다. **역할 분담을 추측하지 않고 먼저 환경을 확인**했습니다.

| 확인 | 결과 | 분담에 미친 영향 |
|---|---|---|
| `gh` CLI | **없음** (`which gh` 실패) | 저장소 생성·설정 변경을 대신 할 수 없음 → 사용자 몫 |
| SSH 인증 | `Hi seedevk8s!` **통과** | 저장소가 생기면 push 는 Claude 가 가능 |
| `tetris2` 존재 | **없음** (`Repository not found`) | 사용자가 먼저 만들어야 함 |
| `subtree split` | **동작함** | 히스토리를 살린 배포가 가능 |
| 파일 경로 | `style.css`·`script.js` 모두 **상대 경로** | `/tetris2/` 하위에서도 그대로 동작 → 고칠 코드 없음 |

마지막 항목이 중요했습니다. Pages 프로젝트 페이지는 주소가 `/저장소이름/` 하위로 들어가서
`/style.css` 같은 절대 경로를 썼다면 전부 404 가 납니다. 이 프로젝트는 처음부터 상대 경로라
`index.html` 을 한 줄도 고치지 않아도 됐습니다.

배포 방식은 두 가지를 적고 **히스토리를 살리는 `git subtree split`** 을 권했습니다.
부트캠프 결과물이라 만들어진 과정이 커밋으로 남는 편이 낫다고 봤습니다.

---

## 9. 저장소 만들었어, 배포 진행해줘

저장소가 비어 있는 것을 확인하고(`git ls-remote` 출력 없음) 방식 A 로 push 했습니다.

```bash
SPLIT=$(git subtree split -q -P src/exercise/chjin/day02/tetris)
git push git@github.com:seedevk8s/tetris2.git "$SPLIT":refs/heads/main
# * [new branch]  da30c0d -> main
```

수업 저장소는 변경되지 않았고, 폴더 내용이 `tetris2` 의 **루트**에 놓였습니다.

**틀린 수치를 하나 고쳤습니다** — `GITHUB_PAGES.md` 에 "24개 커밋이 옮겨진다"고 적었는데
실제로는 **3개**입니다. `git subtree split` 진행 표시의 대괄호 숫자를 커밋 수로 잘못 읽었습니다.
실제 값은 `git rev-list --count` 로 확인했고 문서도 고쳤습니다.
(이 폴더를 건드린 커밋이 3개인 것이 맞습니다 — 최초 구현, BGM 추가, 문서 정리)

배포 주소가 열리려면 사용자가 **Settings → Pages** 에서 소스를 지정해야 합니다.
이 설정은 API 없이는 바꿀 수 없어 넘겼습니다.

---

## 최종 구성

```
day02/tetris/
├── CLAUDE.md          이 폴더의 작업 규약 (git 정책 · 게임 규약 · BGM 규약 · 검증)
├── GITHUB_PAGES.md    GitHub Pages 배포 절차 (역할 분담 · 재배포 · 트러블슈팅)
├── PLAN.md            설계 기준선 — 무엇을 만들기로 했는지
├── README.md          사용법 — 실행 · 조작 · 점수 규칙 · 음악
├── WORKFLOW.md        작업 기록 — 어떻게 거기 도달했는지 (이 문서)
├── index.html         화면 구조
├── style.css          다크 테마 (day01 토큰 재사용)
├── script.js          게임 로직 + BGM
└── test/
    └── logic.test.js  로직 테스트 206항목 (개발용)
```

문서 셋의 역할을 겹치지 않게 나눠 두었습니다 —
**PLAN** 은 기준선, **WORKFLOW** 는 과정, **README** 는 사용법, **CLAUDE** 는 다음 사람을 위한 규약.

## 실행

```bash
cd src/exercise/chjin/day02/tetris
python3 -m http.server 8080     # http://localhost:8080
```

`index.html` 더블클릭(`file://`)으로도 동작합니다 — `fetch` 를 쓰지 않아 CORS 에 걸릴 곳이 없습니다.

## 남은 것

- **브라우저 화면 확인** — 렌더링, 키 입력, 오버레이, 좁은 화면 레이아웃.
  이 환경에 브라우저가 없어 직접 열어봐야 합니다. (로직은 위 206개 테스트로 검증 완료)
- **BGM 실제 청취 확인** — 첫 키 입력에 재생이 시작되는지, 음량·음색이 거슬리지 않는지,
  일시정지·음소거·재시작이 의도대로 동작하는지.
- 홀드 · 고스트 · SRS 월킥 · 효과음 · 최고점수 저장은 의도한 범위 제외 (README 참고)
